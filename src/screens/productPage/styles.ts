import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'red'
  },
  card: {
    width: 300,
    height: 200,
    backgroundColor: "#fff",
    margin: 100
  }
})