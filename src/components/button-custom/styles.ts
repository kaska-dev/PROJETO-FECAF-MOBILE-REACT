import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#000',
    width: '50%',
    height: 52,
    marginTop: 30,
    borderWidth: 1,
    borderRadius: 8,
    justifyContent: 'center',
    alignItems: 'center'
  },
  titleButton: {
    color: "#fff",
    fontSize: 18,
    fontWeight: 600
  }
})